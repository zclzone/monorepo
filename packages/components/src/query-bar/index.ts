import { setupInstall } from '../utils/install'

import QueryBar from './QueryBar.vue'
import QueryBarItem from './QueryBarItem.vue'

setupInstall(QueryBar)
setupInstall(QueryBarItem)

export { QueryBar, QueryBarItem }
